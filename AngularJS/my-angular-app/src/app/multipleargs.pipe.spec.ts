import { MultipleargsPipe } from './multipleargs.pipe';

describe('MultipleargsPipe', () => {
  it('create an instance', () => {
    const pipe = new MultipleargsPipe();
    expect(pipe).toBeTruthy();
  });
});
